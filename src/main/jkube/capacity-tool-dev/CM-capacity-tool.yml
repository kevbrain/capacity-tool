kind: ConfigMap
apiVersion: v1
metadata:
  namespace: {{ocp-namespace}}
  name: capacity-tool-cm-0
  labels:
    app: capacity-tool
data: 
  its4u.environment: "{{its4u.environment}}"
  alert.percentage.consumption.resource.threshold: "{{alert.percentage.consumption.resource.threshold}}"
  app.ocp.instance.name: "{{app.ocp.instance.name}}"
  app.ocp.console.url: "{{app.ocp.console.url}}"
  app.envs: "{{app.envs}}"
  app.envs.namespace.label: "{{app.envs.namespace.label}}"
  app.mail.default.recipient: "{{app.mail.default.recipient}}"
  app.mail.smtp.host: "{{app.mail.smtp.host}}"
  app.mail.smtp.port: "{{app.mail.smtp.port}}"
  app.mail.user: "{{app.mail.user}}"
  app.ocp.server.url: "{{app.ocp.server.url}}"
  app.simulator.reserve.pods: "{{app.simulator.reserve.pods}}"
  app.gonogo.block.request.level: "{{app.gonogo.block.request.level}}"
  app.gonogo.warning.request.level: "{{app.gonogo.warning.request.level}}"
  app-limit.memory: "{{app-limit.memory}}"
  app-limit.cpu: "{{app-limit.cpu}}"
  ocp.environment: "{{ocp.environment}}"
  ocp-namespace: "{{ocp-namespace}}"
  ocp.replicas: "{{ocp.replicas}}"
  ocp-cluster.registry: "{{ocp-cluster.registry}}"
  cluster-suffix: "{{cluster-suffix}}"
  app-actuator.port: "{{app-actuator.port}}"
  app-request.memory: "{{app-request.memory}}"
  app-container.port: "{{app-container.port}}"
  app-request.cpu: "{{app-request.cpu}}"
  app-version: "{{app-version}}"
  prometheus.query.maxovertime: "{{prometheus.query.maxovertime}}"
  prometheus.url: "{{prometheus.url}}"
  project.default.limit.cpu: "{{project.default.limit.cpu}}"
  project.default.limit.memory: "{{project.default.limit.memory}}"
  project.default.request.cpu: "{{project.default.request.cpu}}"
  project.default.request.memory: "{{project.default.request.memory}}"
  app.ocp.worker.prefix: "{{app.ocp.worker.prefix}}"
  app.group1.namespace.label: "{{app.group1.namespace.label}}"
  app.group2.namespace.label: "{{app.group2.namespace.label}}"
  app.group1.button.label: "{{app.group1.button.label}}"
  app.group2.button.label: "{{app.group2.button.label}}"
---
